import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  cirtle_add: {
    fontSize: 50,
    margin: "30px auto",
    color: "#333",
    cursor: "pointer",
    display: "block",
  },
});
